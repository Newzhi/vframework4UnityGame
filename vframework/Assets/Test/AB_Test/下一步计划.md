# AB 加载链路 — 下一步计划

> 基于 2026-06-11 集成测试归档（同步双 Runner 三端 **19/19 PASS**）

---

## 当前结论

### 集成测试（TestABScene · 同步套系）

| 套系 | 内容 | 状态 |
|------|------|------|
| **同步双 Runner** | `Myloadtest` + `MyLoadTest2`，`Load<T>()` | ✅ **Editor** `004641` / `001824` · **Windows Player** `004530` · **Android** `004612`（均 19/0） |
| **同步单 Runner** | 仅 Myloadtest 9 Case | ✅ Editor `004007` · Player `004128` |
| **异步双 Runner** | `MyLoadUniTest` + `MyLoadUniTest2` | ⏳ 脚本就绪，**待跑 `UniConcurrentLoad_*`** |
| **Player 双 Runner** | 同步并发 | ✅ **P0-1 / P0-2 已完成** |

### 框架 API（非集成测试）

| API | 代码 | 集成 Case | 说明 |
|-----|------|-----------|------|
| `Load<T>()` | ✅ | ✅ 同步套系三端 | 主链路已验收 |
| `LoadUniTaskAsync<T>()` | ✅ **基础版** | ⏳ 异步套系待跑 | `UniTask.Yield` + 内部同步 Load |
| `LoadUniTaskWithCallback` 等 | ✅ | ❌ | 无专用 Case |
| CDN / 真异步 I/O | ❌ | ❌ | P2 |

### 其它

- **Player 首包混平台**：`StreamingAssetsPlatformBuildFilter` 已接入  
- **Android jar: Catalogue**：`004612` 验证 StreamingAssetsIO 有效  
- **未覆盖**：Catalogue 非空 `dependencies` 预加载、非法路径 Case、CDN 运行时

---

## P0 — 近期必做

| # | 项 | 状态 | 完成标准 / 基准 |
|---|-----|------|-----------------|
| 1 | **Android 同步双 Runner** | ✅ | `ConcurrentLoad_MyLoadTest2_20260611_004612.json` |
| 2 | **Windows Player 同步双 Runner** | ✅ | `ConcurrentLoad_MyLoadTest2_20260611_004530.json` |
| 3 | **Editor 异步双 Runner** | ⏳ | `UniConcurrentLoad_*`，`passCount=19, failCount=0` |
| 4 | **清理无效日志** | 可选 | 保留 `004612` / `004530` / `004641` / `001824` / `004007` / `004128` |

---

## P1 — 加载能力补全

| # | 项 | 状态 |
|---|-----|------|
| 5 | 跨包并存（model + atlas + ui） | ✅ 三端双 Runner Case 4/5 |
| 6 | 同步并发引用计数 | ✅ Editor + Player + Android |
| 7 | Catalogue 依赖预加载 | ⏳ `bundles[]` 有名称，dependencies 仍空 |
| 8 | LoadByAssetPath / 缓存 | 🟡 MyLoadTest2 Case 2 |
| 9 | 无效路径 Case | ❌ |
| 10 | Shader bundle 依赖 | ❌ |

---

## P2 — 业务 API

| # | 项 | 代码 | 集成 | 说明 |
|---|-----|------|------|------|
| 11 | **LoadUniTaskAsync 集成验收** | ✅ | ⏳ | 跑异步双 Runner → `UniConcurrentLoad_*` |
| 12 | **LoadUniTaskAsync 真异步/CDN** | ❌ | ❌ | 下载队列、后台 I/O |
| 13 | PreLoad 模块预加载 | ❌ | ❌ | |
| 14 | CDN 运行时下载 | ❌ | ❌ | |

---

## P3 — 工程化

| # | 项 |
|---|-----|
| 15 | CI：解析 JSON，`failCount==0` |
| 16 | 清单二进制 / 加密 |
| 17 | EditorTest 策略文档化 |

---

## 建议顺序

```text
P0-3 Editor 异步双 Runner（UniConcurrentLoad JSON）
    ↓
异步双 Runner Player / Android
    ↓
P1-7 Catalogue dependencies + Case
    ↓
P2-12 LoadUniTaskAsync 真异步 / CDN
```

---

## 验收口径

| 类型 | JSON | passCount |
|------|------|-----------|
| 同步单 Runner | `Myloadtest_*.json` | 9 |
| 同步双 Runner | `ConcurrentLoad_*.json` | 19 |
| 异步双 Runner | `UniConcurrentLoad_*.json` | 19 |

**当前同步双 Runner 基准：** `004612`（Android）、`004530`（WindowsPlayer）、`004641`（Editor 最新）、`001824`（Editor 首份）。

---

## 关联

- [集成测试归档.md](./集成测试归档.md)
