# AB 加载链路 — 下一步计划

> 基于 2026-06-10 集成测试归档（Editor + Windows Player 7/7 PASS）

---

## 当前结论

- **DeviceDebug + StandaloneWindows64**：同步 `Load` / `Instantiate` / 换贴图 / 换材质 / Release / UnloadAll **已验收**
- **待验证平台**：Android / iOS 真机（需单独打 AB + 改平台子目录）
- **未覆盖**：异步 API、CDN 热更、跨 bundle 依赖链、EditorTest 仅清单模式

---

## P0 — 近期必做（建议 1～2 周）

| # | 项 | 说明 | 完成标准 |
|---|-----|------|----------|
| 1 | **Android DeviceDebug 打包 + Myloadtest** | 打 Android AB，同场景跑 7 Case | JSON `failCount=0`，日志在 `persistentDataPath` 或 bundle Logs |
| 2 | **清理历史日志** | 删除或移走 `LoadApiTester_*.json` | 仅保留 Myloadtest 有效样本作对照 |
| 3 | **合并 Smoke 测试** | `ABLoadSmokeTest` 与 `Myloadtest` 职责重叠 | 文档标明：集成测用 Myloadtest，Smoke 仅单 API 调试 |

---

## P1 — 加载能力补全

| # | 项 | 说明 | 参考 |
|---|-----|------|------|
| 4 | **跨包依赖用例** | 增加一步：加载带 dependencies 的 bundle（如 UI 包） | `Catalogue.bundles[].dependencies` |
| 5 | **LoadByAssetPath / 缓存** | Myloadtest 增 2～3 Case：同路径二次 Load 同一 `AbstractResource`；`LoadByAssetPath` | `BundleResLoader` 现有 API |
| 6 | **无效路径** | Load 不存在简路径 → 返回 null，日志 FAIL | 防回归 |
| 7 | **Shader 依赖** | tester + ReplaceMat 依赖 URP Shader bundle 时验证依赖预加载 | `AcquireBundleWithDependencies` |

---

## P2 — 业务 API（设计基线）

| # | 项 | 说明 | 文档 |
|---|-----|------|------|
| 8 | **LoadAsync（默认 API）** | `UniTask` 或回调版异步加载 | [业务API与CDN规划.md](../../vFramework/BaseLayer/AssetLayer/ABSystem_Beta/Docs/业务API与CDN规划.md) §1 |
| 9 | **PreLoad 模块预加载** | 按模块名批量 AcquireBundle | 同上 §1 需求4 |
| 10 | **CDN 运行时下载** | `IBundlePathResolver` / HTTP 拉包 + 本地缓存 | 同上 §7；打包侧 CdnHotUpdate 已有 |

---

## P3 — 工程化

| # | 项 | 说明 |
|---|-----|------|
| 11 | **CI 门禁** | Build 后自动跑场景或解析 JSON，`failCount==0` 才通过 |
| 12 | **清单二进制 / 加密** | 评估 JSON → 二进制 + 可选加密（设计 TODO） |
| 13 | **EditorTest 策略** | 明确：仅清单 vs 是否仍需本地 AB；避免「有清单无 bundle」误用 |

---

## 建议执行顺序

```text
Android 真机复测 (P0-1)
    ↓
Myloadtest 补依赖/缓存/非法路径 Case (P1-4～6)
    ↓
LoadAsync 设计与实现 (P2-8)
    ↓
CDN 运行时路径解析 (P2-10)
```

---

## 验收口径（延续当前标准）

- 集成测试：**Myloadtest JSON 全 PASS**，Editor 与目标平台各至少 1 份归档
- 单 API 调试：**ABLoadSmokeTest** 或临时脚本，不替代集成 JSON
- 打包：**DeviceDebug** 产出真实 `.bundle` + 双份 Catalogue，bundle 名小写

---

## 关联归档

- [集成测试归档.md](./集成测试归档.md)
