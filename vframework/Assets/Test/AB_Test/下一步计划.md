# AB 加载链路 — 下一步计划

> 基于 2026-06-10 集成测试归档（Editor + Windows Player **9/9 PASS**，含跨包 atlas/ui）

---

## 当前结论

- **DeviceDebug + StandaloneWindows64**：同步 Load / Instantiate / 换贴图 / 换材质 / **多 bundle 并存** / Release / UnloadAll **已验收**
- **归档样本**：`Myloadtest_20260610_004007`（Editor）、`Myloadtest_20260610_004128`（Player）
- **待验证平台**：Android / iOS 真机
- **未覆盖**：Catalogue 依赖表驱动的 `AcquireBundleWithDependencies`、LoadAsync、CDN 热更、缓存/非法路径 Case

---

## P0 — 近期必做

| # | 项 | 说明 | 完成标准 |
|---|-----|------|----------|
| 1 | **Android DeviceDebug + Myloadtest** | 打 Android AB，跑 **9 Case** | JSON `passCount=9, failCount=0` |
| 2 | **清理无效日志** | 移走 `LoadApiTester_*.json`、旧 7 Case 样本（可选保留） | 以 `004007`/`004128` 为基准 |
| 3 | **Smoke 与集成分工** | 文档/注释标明职责 | 集成 = Myloadtest；调试 = ABLoadSmokeTest |

---

## P1 — 加载能力补全

| # | 项 | 说明 | 状态 |
|---|-----|------|------|
| 4 | **跨包并存** | model + atlas + ui 同时 Load | ✅ Case 4/5 已测 |
| 5 | **Catalogue 依赖预加载** | 打包写入 `bundles[].dependencies`，验证 UI 包依赖 atlas 等 | 清单 `bundles` 仍为空 |
| 6 | **LoadByAssetPath / 缓存** | 二次 Load 同资源；`LoadByAssetPath` | 待加 Case |
| 7 | **无效路径** | 不存在简路径 → null + FAIL | 待加 Case |
| 8 | **Shader bundle** | ReplaceMat / URP 与 shader.bundle 依赖 | 待验证 |

---

## P2 — 业务 API

| # | 项 | 文档 |
|---|-----|------|
| 9 | **LoadAsync** | [业务API与CDN规划.md](../../vFramework/BaseLayer/AssetLayer/ABSystem_Beta/Docs/业务API与CDN规划.md) §1 |
| 10 | **PreLoad 模块预加载** | 同上 §1 需求4 |
| 11 | **CDN 运行时下载** | 同上 §7 |

---

## P3 — 工程化

| # | 项 |
|---|-----|
| 12 | CI：Build 后解析 Myloadtest JSON，`failCount==0` 门禁 |
| 13 | 清单二进制 / 加密 |
| 14 | EditorTest 策略文档化 |

---

## 建议顺序

```text
Android 9 Case 复测 (P0-1)
    ↓
Catalogue 写入 bundle 依赖 + 依赖预加载 Case (P1-5)
    ↓
缓存 / 非法路径 Case (P1-6～7)
    ↓
LoadAsync (P2-9)
    ↓
CDN 运行时 (P2-11)
```

---

## 验收口径

- 集成测试：**Myloadtest 9 Case JSON 全 PASS**，Editor + 目标平台各至少 1 份归档
- 当前基准文件：`Myloadtest_20260610_004007.json`、`Myloadtest_20260610_004128.json`
- 打包：**DeviceDebug** 真实 `.bundle` + Catalogue，bundle 名小写

---

## 关联

- [集成测试归档.md](./集成测试归档.md)
