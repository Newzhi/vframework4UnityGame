# 综合测试归档（comprehensiveTest）

> 归档日期：2026-06-14  
> 场景：`StartGame` → `PoolTest`  
> 验证：**GameEventBus**、**PrefabPool** / `BundleResLoader.GetOrCreatPool`、玩家生命与复活、日志导出、**池所有权（谁创建谁销毁）**

### 池所有权约定（无全局 PoolHost）

| 组件 | 职责 |
|------|------|
| `PlayerTest` | 首次射击时 `GetOrCreatPool` 子弹池；若先建则 `OnDestroy` 对称销毁 |
| `enemyManager` | 建敌人池 + 活跃根；`OnDestroy` 销毁敌人池 |
| `enemyTest` | 射击时懒加载共享子弹池（`GetOrCreatPool` 去重）；实例不卸池 |
| `ComprehensiveTestSceneFlow` | 切场景 / ExitGame 前 `CleanupBeforeSceneChange()`：`UnloadAll` + 销毁 `PoolRuntime` |

---

## 1. 归档日志索引

| 文件 | 平台 | 场景 | 时长/说明 |
|------|------|------|-----------|
| `Logs/archive/ComprehensiveTest_PoolTest退出_20260614_025017.log` | **WindowsPlayer** ★ | PoolTest | **~503s**；死亡/复活已记录；对象池/内存健康 |
| `Logs/archive/ComprehensiveTest_PoolTest退出_20260614_020534.log` | WindowsEditor | PoolTest | ~330s；旧 Logger 截断 |
| `Logs/archive/ComprehensiveTest_StartGame退出_20260614_023026.log` | WindowsPlayer | StartGame | 退出采样；bundle 路径验证 |

日志导出约定见 `ComprehensiveTestLogExporter`：Editor → `StreamingAssets/{平台}/Logs/`；真机回退 → `persistentDataPath/comprehensiveTest/Logs/`。

---

## 2. PoolTest 会话结论（WindowsPlayer · 025017）★ 有效基准

### 环境

| 项 | 值 |
|----|-----|
| Unity | 2022.3.62f3c1 |
| 平台 | WindowsPlayer（`winbao` 独立构建，非 Editor） |
| 导出目录 | `winbao_Data/StreamingAssets/StandaloneWindows64/Logs` |
| DeviceLogPath | `AppData/LocalLow/DefaultCompany/vframework/comprehensiveTest/Logs` |
| 对局时长 | **约 503 秒**（日志 951 行，自 1.1s 起完整） |
| 退出内存 | mono **3.0 MB**（较基线 **+964 KB**） |

### 对象池 ★ PASS

| 池 | 峰值 b/p | 退出 b/p | 说明 |
|----|----------|----------|------|
| Bullet | 34 / 38 | 13 / 25 | `maxInactive=48` 生效；闲置未超上限 |
| Enemy | 8 / 8 | 5 / 3 | 峰值 8 符合上限；死亡回池 |

- 多次出现 `Bullet b=0 p=38`：场上无借出、闲置满池复用，**正常**
- `actE=8` 与 `borrowed=8` 一致

### 事件 / 生命 ★ PASS

| 时间 | 事件 |
|------|------|
| 332.0s | 玩家死亡 lives=2 → 复活 `303,1,-10` |
| 405.7s | 玩家死亡 lives=1 → 复活 `306,1,-39` |

新版 Logger Pin 机制验证通过。

### 内存 ★ PASS

- 全程 mono **2.0～3.1 MB**，无 MEM 警告
- `reserved` 稳定 **147.8 MB**
- 对比 Editor 773 MB：Player 基线正常，**无托管泄漏迹象**

### 验收

| 项 | 状态 |
|----|------|
| EnsureReady | ✅ |
| 共享 Bullet 池（玩家+敌人） | ✅ |
| 玩家死亡/复活 | ✅ |
| 日志导出 Windows Player | ✅ |
| 长测 8+ 分钟完整性 | ✅ |

---

## 3. PoolTest 会话结论（Editor · 020534）

### 环境

| 项 | 值 |
|----|-----|
| Unity | 2022.3.62f3c1 |
| 导出目录 | `Assets/StreamingAssets/StandaloneWindows64/Logs` |
| 退出内存 | mono **773 MB**（较基线 **+95 MB**） |

> 注：该日志为旧版 Logger（500 行截断），正文从 **282.5s** 起，**前半段（含可能的死亡/复活）已丢失**。新版 Logger 已 Pin 重要事件并扩容至 12000 行。

### 对象池 ★

| 池 | 峰值 borrowed | 峰值 pooled | 退出 borrowed/pooled | 结论 |
|----|---------------|-------------|----------------------|------|
| Bullet | 31 | 44 | 1 / 44 | 借出/归还正常；子弹约 4s 寿命后回池 |
| tester | 8 | 7 | 2 / 6 | 与 `MaxActiveEnemies=8` 一致；死亡后 Release |

Hierarchy：`PoolRuntime` 下 4 节点（Inactive/Active × Bullet/Enemy）结构正确。

### 事件总线 ★

- 玩家/敌人射击、受伤、死亡、敌人生成：**持续产出**
- 玩家多次 **-8** 受伤；**未见**「玩家死亡/复活」行（截断 + 可能 HP 未归零于可见段）

### 内存 ⚠

- `reserved` 较基线 **+16 MB**（稳定）
- `mono/gc` **+95 MB**：Editor 基线高 + 高频日志字符串；需结合静止复测与新版精简日志再判泄漏
- 周期警告：MonoUsed 近 6 次采样上升（战斗期正常波动，非退出时 borrowed 堆积）

### 验收

| 项 | 状态 |
|----|------|
| BundleResLoader + 双池共享（玩家/敌人子弹） | ✅ |
| PrefabPool Get/Release | ✅ |
| 敌人上限 8 | ✅ |
| 日志导出 Editor 路径 | ✅ |
| 玩家长测日志完整性 | ⚠ 旧版截断；已修复 |

---

## 4. StartGame 会话结论（WindowsPlayer · 023026）

| 项 | 值 |
|----|-----|
| 导出目录 | `winbao_Data/StreamingAssets/StandaloneWindows64/Logs` |
| DeviceLogPath | `AppData/LocalLow/DefaultCompany/vframework/comprehensiveTest/Logs` |
| 退出内存 | mono **2.7 MB** |

**结论**：Windows 独立构建日志写入 bundle 根 `Logs` 成功；`persistentDataPath` 回退路径已写入文件头。

---

## 5. 真机日志路径速查

| 平台 | 路径 |
|------|------|
| Android（典型） | `/data/data/<包名>/files/comprehensiveTest/Logs/` |
| Android（部分机型） | `/storage/emulated/0/Android/data/<包名>/files/comprehensiveTest/Logs/` |
| Windows Player | `{exe}_Data/StreamingAssets/{平台}/Logs/` 或上表 persistent 路径 |

```bash
adb pull "/storage/emulated/0/Android/data/<包名>/files/comprehensiveTest/Logs/" ./
```

---

## 6. 后续建议

1. 用新版 Logger 重跑 **≥10 分钟**，确认「玩家死亡 / 复活」Pin 行存在  
2. 对象池：已加 **闲置上限** 与 **根节点缓存**，避免子弹池无限膨胀  
3. 内存：战斗结束后静止 30s，看 `borrowed` 与 mono 是否回落
