# ABSystem_Beta 主路线

> **唯一排期与方向文档**（2026-06-08 定稿）  
> 进度细节见 [设计目标与实现细节.md](./设计目标与实现细节.md)；集成测试见 [集成测试归档.md](../../../../Test/AB_Test/集成测试归档.md)。

---

## 1. 原则（不换方向）

1. **已验收的主链路不动**：`打包 → 清单 → BundleResLoader 同步 Load + 双层 Ref` 是地基，不推倒重来。  
2. **在现有 API 上扩展**：业务入口仍是 `BundleResLoader.Instance` + `IAssetHandle`；异步走 `LoadUniTaskAsync`，CDN 走 `BundleManager` 扩展点，不另起一套 ResourceManager。  
3. **不做四不像**：不混用旧框架 API、不为了通用指南单独改卸包策略（`Unload(true)` 维持至有真机数据再评估）。  
4. **测试门禁**：任何加载侧改动不得破坏同步双 Runner **三端 19/19**（基准 JSON 见集成测试归档 §6）。

---

## 2. 主路线（三阶段）

```text
【阶段 A · 已完成】打包 + 清单 + 同步 Load + 三端双 Runner 19/19
        │
        ▼
【阶段 B · 进行中】异步集成验收 → 真异步 / inFlight 合并
        │
        ▼
【阶段 C · 下一阶段】CDN 运行时（路径解析 + 下载 + 缓存 + 清单 version 比对）
        │
        ▼
【远期 · 禁止区产品场景】边玩边下 / DLC / MOD / 分工程（依赖阶段 C）
```

| 阶段 | 目标 | 验收 |
|------|------|------|
| **A** | 规则制定器 + 打包器 + Catalogue + 同步 API | `ConcurrentLoad_*` passCount=19（Editor / Player / Android） |
| **B-1** | `LoadUniTaskAsync` 集成（API 壳 + 并发语义） | `UniConcurrentLoad_*` passCount=19 |
| **B-2** | 真异步 I/O、同 path inFlight 合并、完成时 ref==0 丢弃 | 新 Case + 不回归阶段 A |
| **C-1** | `IBundlePathResolver` 多根目录（首包 / persistentDataPath） | 本地缓存命中 Load |
| **C-2** | `IRemoteBundleProvider` 清单对比 + HTTP 下载 | CdnHotUpdate 产物可拉取并 Load |
| **C-3** | 首包 / 远程分包策略上线 | 非全部 AB 进 StreamingAssets |

---

## 3. 当前状态（快照）

| 项 | 状态 |
|----|------|
| 同步 `Load<T>` + Release / UnloadAll | ✅ 三端双 Runner 19/19 |
| 打包三种模式 + Player 平台过滤 | ✅ |
| `LoadUniTaskAsync` / 回调 API | ✅ 代码有；**集成 JSON 待归档** |
| CDN 打包产出 | ✅ `cdnOutputPath` |
| CDN 运行时下载 | ❌ `BundleManager` TODO(CDN) |
| `PreLoad` / `AssetRouter` | ❌ 占位 |
| 清单 `version` / `buildNumber` 运行时比对 | ❌ 仅打包写入 |

---

## 4. 排期（按顺序执行）

### P0 — 异步集成（阶段 B-1）

| # | 项 | 完成标准 |
|---|-----|----------|
| 1 | Editor 异步双 Runner | `UniConcurrentLoad_*`，passCount=19 |
| 2 | Player / Android 异步双 Runner | 各平台至少 1 份 JSON |

**操作**：`TestABScene` 切换异步套系（见集成测试归档 §3）。

### P1 — 加载补全（与 B/C 并行，非主路线阻塞）

| # | 项 | 说明 |
|---|-----|------|
| 3 | Catalogue 非空 dependencies Case | 依赖预加载单独验 |
| 4 | 无效路径 Case | 异常路径 |
| 5 | Custom 规则集成 JSON | 可选 |

### P2 — 异步内核 + CDN（阶段 B-2 + C）

| # | 项 | 模块 |
|---|-----|------|
| 6 | inFlight 合并 + ref==0 完成丢弃 | `BundleResLoader` |
| 7 | 真异步 Load / 下载队列 | `BundleResLoader` + UniTask |
| 8 | `IBundlePathResolver` | `BundleManager` |
| 9 | `IRemoteBundleProvider` + version 比对 | 见 [业务API与CDN规划.md](./业务API与CDN规划.md) §2 |
| 10 | `PreLoad` 包级预热 | `BundleResLoader` |

### P3 — 工程化

| # | 项 |
|---|-----|
| 11 | CI 解析 JSON，failCount==0 |
| 12 | 清单二进制 / 加密（可选） |

### 延后（不阻塞主路线）

| 项 | 说明 |
|----|------|
| DestroyInstance / AutoUnload | 释放体验优化；业务当前用「保存句柄 + Release」即可 |
| 延迟卸载队列（15~30s） | Common 常驻可用启动 Load 不 Release |
| `Unload(false)` 两阶段 | 有内存 profiling 再评估 |
| AssetRouter 多后端 | CDN 落地后再并 Resources 入口 |

---

## 5. 业务 API 约定（稳定面）

**加载（推荐）**

```csharp
IAssetHandle h = BundleResLoader.Instance.Load<GameObject>("UI/UIRoot");
GameObject go = h?.Instance;
// 异步：await LoadUniTaskAsync<T>(path)
```

**卸载**

```csharp
h?.Release();  // 或 Unload(h, go)；勿与 Release 重复
BundleResLoader.Instance.UnloadAll();  // 仅切场景/关游戏
```

详见 [业务API调用指南.md](./业务API调用指南.md)。

**Common 等资源**：无单独常驻策略；需长期占用则 **Load 一次且不 Release**，或等 `PreLoad`（P2-10）。

---

## 6. 集成测试门禁

| 套系 | JSON 前缀 | passCount |
|------|-----------|-----------|
| 同步单 Runner | `Myloadtest_*` | 9 |
| 同步双 Runner | `ConcurrentLoad_*` | **19**（回归基准） |
| 异步双 Runner | `UniConcurrentLoad_*` | **19**（阶段 B-1） |

同步基准：`004612`（Android）、`004530`（Player）、`004641`（Editor）。

---

## 7. 文档地图

| 文档 | 角色 |
|------|------|
| **本文（主路线.md）** | 方向 + 排期 + 门禁 |
| [设计目标与实现细节.md](./设计目标与实现细节.md) | 禁止区基线 + 模块进度 checklist |
| [业务API与CDN规划.md](./业务API与CDN规划.md) | CDN / 异步扩展设计细节 |
| [业务API调用指南.md](./业务API调用指南.md) | 业务抄用范式 |
| [Catalogue清单说明.md](./Catalogue清单说明.md) | 清单字段 |
| [加载器设计说明.md](../ResLoader/加载器设计说明.md) | 双层架构 |
| [集成测试归档.md](../../../../Test/AB_Test/集成测试归档.md) | Case + JSON 基准 |
| [ResourceSystemDesignGuide.md](../../../../Resources/ResourceSystemDesignGuide.md) | 外部通用参考（**不驱动本项目改方向**） |

**维护约定**：排期、阶段状态只改 **主路线.md** 与 **设计目标·实现细节区**；子模块 `说明.md` 只写模块职责，不写总排期。

---

## 8. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-06-08 | 定稿主路线：不换同步地基，阶段 B 异步 → 阶段 C CDN；合并原「下一步计划」「通用资源设计对照」 |
